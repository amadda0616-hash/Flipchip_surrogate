RYT
사용자께서는 업로드한 Jupyter Notebook 파일(`Tabula+Bayesian.ipynb`)에 쓰인 기법들의 이름과 그 기법들에 대한 설명을 요청하셨습니다. 파일 내용을 살펴보면, 딥러닝 기반 대리 모델링 및 최적화 과정이 포함되어 있습니다. 코드를 분석하여 사용된 주요 기법들을 식별하고 한국어로 설명해 드리겠습니다.

---

제공해주신 코드는 반도체 플립칩(Flipchip) 패키징 시뮬레이션 데이터를 바탕으로 **신뢰성(WarpMax, Peel Stress 등)을 예측하고 최적의 설계 변수(P1~P6)를 도출하기 위한 AI 기반 대리 모델링 및 역설계 최적화 파이프라인**입니다.

이 코드에 사용된 주요 머신러닝/딥러닝 및 최적화 기법들과 그 설명은 다음과 같습니다.

### 1. 대리 모델 (Surrogate Model) 기법

무겁고 시간이 오래 걸리는 물리/FEA(유한요소해석) 시뮬레이션을 대체하기 위해, 기존 시뮬레이션 데이터를 바탕으로 입력(설계 변수)과 출력(응력/변형 결과) 간의 관계를 학습하는 빠르고 가벼운 AI 모델을 만드는 기법입니다.

* **Tabular ResNet (표 형식 데이터용 ResNet)**
* **설명**: 이미지 처리에 널리 쓰이는 ResNet(Residual Network) 아키텍처를 1차원 표 형식(Tabular) 데이터에 맞게 변형한 딥러닝 모델입니다. 다층 신경망(MLP)에 Skip Connection(잔차 연결)을 추가하여 기울기 소실 문제를 완화하고 깊은 층에서도 안정적이고 강력한 비선형 학습이 가능하게 합니다.
* **코드 내 역할**: 설계 변수 6개(P1~P6)를 입력받아 15개의 물리적 예측 성능 지표(Y 채널, 예: 휨, 응력 등)를 동시에 예측하는 다중 출력 회귀(Multi-output Regression) 모델로 사용되었습니다.



### 2. 하이퍼파라미터 최적화 및 탐색 기법

* **Bayesian Optimization (베이지안 최적화) - TPE 알고리즘**
* **설명**: 미지의 목적 함수를 최대화 또는 최소화하기 위해 확률 모델(가우시안 프로세스 등)을 사용하여 최적의 입력값을 탐색하는 기법입니다. Optuna 라이브러리를 통해 구현되었으며, 그 중에서도 TPE(Tree-structured Parzen Estimator) 샘플러가 사용되었습니다. TPE는 과거의 탐색 결과를 바탕으로 우수한 결과를 낼 확률이 높은 영역을 집중적으로 탐색하여 효율성을 높입니다.
* **코드 내 역할**: Tabular ResNet 대리 모델을 바탕으로, WarpMax(휨)와 T_Tip_Peel(박리 응력) 등을 최소화하는 최적의 설계 변수 조합(P1~P6) 1,500개를 빠르고 지능적으로 탐색하는 데 사용되었습니다.



### 3. 다목적 최적화 (Multi-Objective Optimization) 기법

* **NSGA-II (Non-dominated Sorting Genetic Algorithm II)**
* **설명**: 유전 알고리즘(Genetic Algorithm)의 일종으로, 서로 상충하는 두 개 이상의 목적(예: 휨 최소화와 박리 응력 최소화)을 동시에 최적화할 때 사용됩니다. 각 세대에서 우수한 해를 '파레토 비지배 정렬(Non-dominated Sorting)'을 통해 분류하고, '혼잡도 거리(Crowding Distance)'를 계산하여 해들이 특정 영역에 몰리지 않고 골고루 퍼지도록(다양성 유지) 합니다.
* **코드 내 역할**: `pymoo` 라이브러리를 사용하여, 다수의 목적 함수(15개 지표 중 핵심 타겟들)를 동시에 최소화하는 수많은 최적의 타협점, 즉 '파레토 프론티어(Pareto Frontier)' 데이터 셋을 도출하기 위해 사용되었습니다.



### 4. 기타 주요 머신러닝 기법 및 데이터 처리 기법

* **Autoencoder (오토인코더)**
* **설명**: 입력 데이터를 압축하여 중요한 특징만을 추출하는 인코더(Encoder)와, 압축된 잠재 벡터(Latent Vector)를 다시 원래의 차원으로 복원하는 디코더(Decoder)로 구성된 비지도 학습 모델입니다.
* **코드 내 역할**: 복잡한 617개의 시계열 형태를 가진 타겟 텐서를 저차원(32차원)의 잠재 공간(Latent Space)으로 압축하고 매핑하여 안정적인 역설계(Inverse Design)를 수행하는 데 활용되었습니다 (코드 주석 및 흐름 상 등장).


* **Permutation Feature Importance (순열 변수 중요도)**
* **설명**: 특정 변수(Feature)의 값을 무작위로 섞었을 때 모델의 예측 성능이 얼마나 떨어지는지를 측정하여, 해당 변수가 모델 예측에 얼마나 중요한 역할을 하는지 평가하는 기법입니다.
* **코드 내 역할**: Tabular ResNet 대리 모델이 예측을 수행할 때 P1~P6 중 어떤 설계 변수가 핵심 타겟(WarpMax 등)에 더 민감하게 영향을 미치는지 분석(민감도 분석)하기 위해 사용되었습니다.


* **MinMaxScaler (최소-최대 정규화)**:
* **적용**: 설계 변수(P1~P6) 및 단일 성능 지표(WarpMax 등).
* **이유**: 설계 변수는 물리적인 하한선과 상한선(Bounds)이 명확합니다. 이를 0과 1 사이로 맞추면, **베이지안 최적화** 알고리즘이 정해진 탐색 공간 내에서 효율적으로 움직일 수 있습니다.


* **StandardScaler (표준 정규화)**:
* **적용**: 복잡한 시계열 데이터(7개 채널의 Stress/Strain 텐서).
* **이유**: 시계열 데이터는 값의 범위보다 **패턴과 변동성**이 중요합니다. 평균을 0, 분산을 1로 맞춤으로써 특정 채널의 값이 너무 커서 모델 학습이 왜곡되는 것을 방지하고, **오토인코더**가 특징을 더 잘 추출하게 돕습니다.